## 1. Application skeleton

- [x] 1.1 Create the minimal Electron stable v44, React 19.3, and Vite 8.3 project skeleton with build metadata and no product feature screens; verify the dependency graph and repository contain no private data.
- [x] 1.2 Add a production/local startup path using built local renderer assets that does not require a server or network; validate startup with network access unavailable and record the result.

## 2. Process and writer boundary

- [x] 2.1 Establish the React renderer → narrow preload/IPC → Electron main/application/domain boundary; verify renderer code has no direct database or private-filesystem write access.
- [x] 2.2 Enforce the per-normalized-root ownership lifecycle: acquire authority before opening write-capable persistence or accepting canonical writes, fail closed with an explicit conflict for a concurrent same-root instance, allow different valid roots to operate independently, release ownership on clean shutdown, and recover stale/crash ownership deterministically while failing closed on uncertainty; document external direct editing as unsupported without claiming global prevention and add boundary validation.

## 3. Private-root contract

- [x] 3.1 Implement user-configured absolute private-root validation outside the repository, including normalized-path checks, root identity/binding checks, explicit failure for unset, unavailable, unreadable, unsafe, or repository-local roots, and non-sensitive runtime configuration for the selected root; validate with synthetic roots and no fallback outside the selected root.
- [x] 3.2 Initialize exactly one foundation-managed state location under a valid private root; prove first initialization publishes one ready store with stable identity/version metadata, repeat initialization reuses it without reset or replacement, different roots remain separate, and partial failure/retry leaves no false success or repository-local fallback.

## 4. Structured persistence bootstrap

- Implementation order is: complete the application skeleton and dependency
  resolution, run 4.2, and only if its probe passes implement and accept the
  runtime persistence path in 4.1; the numbering is retained for continuity
  with the existing change.
- [x] 4.1 Add the narrow persistence boundary and SQLite bootstrap through `better-sqlite3` 13.0.3 in the main process, including the minimum store identity, root binding, `FOUNDATION_STORE_VERSION = 1` support boundary, and ready metadata; validate first/repeat initialization, deterministic partial-failure retry, transaction-safe failure, unsupported-version fail-closed behavior, reopen, and foundation-only non-persistence. Do not accept this persistence path until 4.2 passes.
- [x] 4.2 After the skeleton exists and dependencies resolve, configure `@electron/rebuild` and run the synthetic Electron native-binding open/write/transaction/reopen probe before accepting 4.1; stop and report the mismatch and reassess the selected binding/build configuration if the compatibility gate fails, without silently replacing SQLite or `better-sqlite3`.

## 5. Foundation validation and handoff

- [x] 5.1 Add deterministic `node:test`/`node:assert/strict` fixtures using isolated synthetic private roots for offline startup, valid/invalid private-root handling and no fallback, first/repeat/reopen initialization, partial-failure retry, unsupported store version, one writer/same-root conflict/different-root independence, clean release, stale/crash recovery, SQLite open/write/transaction/reopen, metadata/version reads, renderer write isolation, no product-domain data, and repository privacy checks.
- [x] 5.2 Run foundation validation from an isolated validation state or bounded implementation-owned paths; record the approved pre-validation status, distinguish known dirty OpenSpec planning edits from unexpected implementation/runtime dirt, and prove validation introduces no unexplained mutations or product M1 behavior. Record the evidence in the existing change record/validation output before starting the next M1 OpenSpec change; do not require the entire repository worktree to be clean.

## M1 Foundation hardening follow-up (2026-09-16)

- [x] H1 Reject pre-existing symlinked or out-of-root `.career2` and foundation state paths, and fail closed when validation observes an identity/path anomaly. Per the user's 2026-09-16 threat-model decision, adversarial same-user replacement between validation and the filesystem open syscall is an M1 non-goal and remains future hardening, not a claimed fix.
- [x] H2 Serialize stale-owner recovery atomically, preserve exclusive final ownership, verify ownership through READY publication, give recovery claims an owner/generation/staleness lifecycle that survives claimant termination, and add simultaneous and crash/takeover regressions.
- [x] H3 Expose only persistence metadata and `close()` to callers; keep the raw SQLite handle and path private to the implementation.
- [x] H4 Synchronize the affected existing project-state documents without declaring M1 accepted or changing product scope.
- [x] H5 Complete this follow-up validation matrix, including an observed real Electron startup with the hardened ownership code, then record the final Git mutation comparison.

## Acceptance evidence

- 1.2 (2026-09-15): An approved Electron 44.3.0 process loaded the actual src/main/index.cjs entry through a temporary observer. The main process reached ready, created a BrowserWindow, loaded dist/index.html from file://, and rendered status ready. The preload exposed only careerFoundation.getStatus; renderer require, process, fs, and database globals were unavailable. HTTP(S) requests were blocked and none were attempted. Synthetic userData selected the synthetic private root; the foundation store reached READY, version 1. The app exited with code 0 and released foundation.lock. All probe/runtime files were outside the repository.
- 5.2 (2026-09-15): Foundation tests passed 11/11; Electron 44.3.0 / embedded Node 24.20.0 / better-sqlite3 13.0.3 ABI probe passed; Vite build passed; OpenSpec strict validation passed 7/7. `git diff --check` passed. The pre-final-validation and final `git status --short` path sets matched; known dirty planning/docs and pre-existing implementation paths remained unchanged. Implementation-file SHA-256 fingerprints and the tracked diff fingerprint excluding this task ledger matched their pre-validation values, so tests/build introduced no source mutations or product M1 behavior. An earlier status capture listed runtime-data-flow-map.md as deleted; it was modified in the pre-final-validation and final snapshots, with its historical-reference diff timestamped 2026-09-14 and unchanged during this run. No private data, dependency/version changes, or scope expansion; no commit created.
- H1–H3 (2026-09-16): H1 closes under the user's approved M1 threat model: pre-existing `.career2` and foundation-file symlink/hard-link cases are rejected; identity/path anomalies observed during validation fail closed. A deterministic same-user replacement at lock-open remains unmitigated, explicitly out of scope, and retained as a skipped future-hardening fixture; no claim is made that the race is fixed. Darwin validates both ownership state paths before its kernel lock; other platforms use an exclusive recovery claim plus exclusive owner creation, with a generation-bound claim that can be atomically taken over after claimant termination. Ownership is checked during initialization and before return. `initializeFoundationStore()` exposes `{ metadata, close }` only.
- H4 (2026-09-16): `PLAN.md`, `PROGRESS.md`, `docs/PRODUCT_PLAN.md`, `docs/DECISION_LOG.md`, `docs/OSS_REUSE.md`, `docs/architecture/CURRENT_ARCHITECTURE.md`, and this active change record now distinguish implementation/hardening from pending read-only M1 acceptance review or historical T01/R1 states. `docs/VALIDATION_SUMMARY.md` remains unchanged because it explicitly owns only a historical 2026-09-13 snapshot. Product scope and selected dependency versions are unchanged.
- H5 current evidence (2026-09-16): the test-only acceptance probe uses a temporary CommonJS module-loader view to observe the actual production `BrowserWindow` constructor because this Electron runtime does not expose `preload` through `getLastWebPreferences()`; production runtime behavior, dependencies, and versions were unchanged. Foundation tests 17 passed, 1 skipped (same-user path-swap future hardening), 0 failed; Vite build PASS; OpenSpec strict validation PASS; `git diff --check` PASS. Fresh PID 79150 loaded `src/main/index.cjs`, reached ready-to-show with `isVisible=true`, `isMinimized=false`, `isDestroyed=false`, and 900x680 bounds, loaded the local renderer, exercised preload and the narrow `getStatus` IPC capability, and initialized the synthetic private root with READY store version 1 plus expected store identity/root binding. The probe held an 8-second visible observation window; the user observed the Career 2.0 window during that interval. The process exited code 0 / signal null and the probe reacquired writer ownership after shutdown. No approval prompt was observed for the final run. H5 = PASS; read-only M1 acceptance review remains pending. No private data, dependency/version changes, scope expansion, or commit.
- Ownership final-hardening evidence (2026-09-16): the Darwin-specific regression rejected pre-existing symlinked and hard-linked `foundation.lock.recovery` paths before lock acquisition and left outside sentinels unchanged. A forced non-Darwin two-process regression created a recovery claim, terminated the claimant abnormally, then had a second process atomically take over the stale claim, initialize the synthetic store, hold one valid owner lock, release it, and reacquire the root. Full foundation results were 17 passed, 1 skipped, 0 failed.
- Project State Sync (2026-09-16): current ownership/runtime status was reconciled in `PLAN.md`, `PROGRESS.md`, `docs/PRODUCT_PLAN.md`, `docs/DECISION_LOG.md`, `docs/OSS_REUSE.md`, `docs/architecture/CURRENT_ARCHITECTURE.md`, `docs/architecture/diagrams/authority-ownership-map.md`, `docs/architecture/diagrams/workspace-boundary-map.md`, and the frozen historical delivery plan. Historical `docs/VALIDATION_SUMMARY.md` and the historical runtime-flow diagram were not rewritten.
